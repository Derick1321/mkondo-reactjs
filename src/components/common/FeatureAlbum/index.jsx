import React, { useState } from 'react'
import styles from './index.module.scss';

export const FeatureAlbum = (props) => {
    //props
    const { album } = props;

    //state
    const [cover, setCover] = useState();

    //effects

    //handlers
    const handlePlay = () => {}
    const handleOpen = () => {}

    return (
        <div>Feature Album</div>
    );
}
